
import { getTerminalSize } from './terminal'



describe('terminal', () => {

    describe('getTerminalSize', () => {
        it('should be a function', () => {
            expect(getTerminalSize).toBeInstanceOf(Function)
        })
    })

})